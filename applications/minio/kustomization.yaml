apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  # secret.yaml removed — credentials in ../secrets.enc.yaml (sops encrypted)
  - route.yaml
