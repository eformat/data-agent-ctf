---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: trino-coordinator
spec:
  to:
    kind: Service
    name: trino
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
