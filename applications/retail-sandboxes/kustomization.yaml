---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sandbox-crs.yaml
  - sandbox-job.yaml
configMapGenerator:
  - name: sandbox-policy
    files:
      - policy-retail.yaml
generatorOptions:
  disableNameSuffixHash: true
