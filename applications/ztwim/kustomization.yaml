---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ZTWIM deployment is done via separate ArgoCD Applications
# (see app-of-apps/apps/ztwim.yaml) — ZTWIM via kustomize.
# This kustomization exists for local validation only.
resources:
  - ztwim/
